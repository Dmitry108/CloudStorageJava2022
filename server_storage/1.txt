Начал работу над проектом: "Облачное хранилище"
1. Организовал клиент-серверное взаимодействие при помощи библиотеки java.io
2. Серверная сторона понимает команду завершения работы клиента и освобождает ресурсы
3. Клиент разрабатывается с помощью JavaFX, связаны между собой классы запуска окна, контроллера этого окна и view
4. Спроектировал основу пользовательского интерфейса, который содержит две файловые таблицы, одна для локального
пространства памяти, другая будет для серверного хранилища:
   - настроил "локальную" таблицу со столбцами имени файла и указания размера в байтах, причем для папок размер не указывается,
   - для записи в таблице создал сущность FileInfo, который пока содержит два поля (имя и размер),
   - обеспечил заполнение таблицы файлами из указанного пути,
   - вынес логику соединения с сервером из контроллера в отдельный класс NetIo,
   - класс контроллера будет являться посредником между view и классами логики,
   - скорее всего в классе NetIo оставлю только работу по сетевому взаимодействию,
   а бизнес-логику перенесу еще в отдельный класс, чтобы изменении методов работы с сетью не влияло на работу классов бизнес-логики,
   - организовал выход из клиентской программы из меню File -> Exit, также можно выйти закрытием окна,
   при этом событие выхода перехватывается и отправляется команда выхода на сервер, чтоб там освободить ресурсы.

   pullreques с проделанной работой https://github.com/Dmitry108/CloudStorageJava2022/pull/1

Ближайшие планы к следующему дз:
 - найти способ получить доступ к файловой системе облачного хранилища, чтобы заполнить вторую таблицу файлов,
 - изучить библиотеку nio и заменить File(java.io) на Path и Files(java.nio),
 - продумать передачу файлов потоком битов,
 - начать оформлять протокол общения между клиентом и сервером.